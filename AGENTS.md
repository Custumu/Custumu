# Agent Rules and Instructions

## Prohibited Commands
- **NEVER** run `npm run build` under any circumstances.
- Use only the already running dev server or lint checks without triggering production builds.
